VITE_API_URL=http://localhost:3001

# Operational toggle for the NFL live tracker tab (Sprint 9.2). Defaults ENABLED
# when unset; set to false to hide the NFL Live tab and stop game-time polling.
# VITE_NFL_LIVE_TRACKER_ENABLED=false
